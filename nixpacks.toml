[phases.setup]
nixPkgs = ['nodejs_20', 'postgresql']

[phases.install]
cmds = ['cd server && npm install']

[phases.build]
cmds = ['cd server && npm run build']

[start]
cmd = 'cd server && npm run migrate && npm start'
